import { AccessabilityHighlight } from './accessability-highlight';

describe('AccessabilityHighlight', () => {
  it('should create an instance', () => {
    const directive = new AccessabilityHighlight();
    expect(directive).toBeTruthy();
  });
});
