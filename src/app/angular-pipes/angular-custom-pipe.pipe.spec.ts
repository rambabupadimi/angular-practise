import { AngularCustomPipePipe } from './angular-custom-pipe.pipe';

describe('AngularCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AngularCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
