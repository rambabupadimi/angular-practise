import { ClickEmitDirective } from './click-emit.directive';

describe('ClickEmitDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickEmitDirective();
    expect(directive).toBeTruthy();
  });
});
