import { FontWeightDirective } from './font-weight.directive';

describe('FontWeightDirective', () => {
  it('should create an instance', () => {
    const directive = new FontWeightDirective();
    expect(directive).toBeTruthy();
  });
});
