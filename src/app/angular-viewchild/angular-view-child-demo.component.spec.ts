import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AngularViewChildDemoComponent } from './angular-view-child-demo.component';

describe('AngularViewChildDemoComponent', () => {
  let component: AngularViewChildDemoComponent;
  let fixture: ComponentFixture<AngularViewChildDemoComponent>;

  beforeEach(async () => {
    // await TestBed.configureTestingModule({
    //   imports: [AngularViewChildDemoComponent],
    // }).compileComponents();

    // fixture = TestBed.createComponent(AngularViewChildDemoComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
   // expect(component).toBeTruthy();
  });
});
