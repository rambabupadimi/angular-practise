import { AngularPiPipePipe } from './angular-pi-pipe-pipe';

describe('AngularPiPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AngularPiPipePipe();
    expect(pipe).toBeTruthy();
  });
});
